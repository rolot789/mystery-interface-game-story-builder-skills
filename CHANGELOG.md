# 변경 내역

## 1.1.0 — 2026-09-25

세계관을 세밀하되 모순 없이 설계하도록 World Builder를 다시 쓰고, 이를 받치는 Canon schema v2와 자동 검사를 추가했다. 분석과 계획은 [docs/improvement-plan.md](docs/improvement-plan.md)에 있다.

### 세계관 설계

- **World Builder 재작성.**
  - 꼬임 방지 8원칙과 세부 서술 절차를 넣었다. 세부 서술 절차는 쓰기 전에 고정점을 확인하고, 쓴 뒤에 서술 속에 생긴 암묵 설정을 회수하는 방식이다.
  - 6층 설계: 헌장 → 기반 → 제도 → 정보 환경 → 생활 → 사건 접점.
  - 참조 문서 8종으로 나눴다. 기존 `world.md`의 내용은 새 문서들로 옮겼다.
- **설정 밀도·공개도·출처.** CORE / SUPPORTING / MENTION, PUBLIC / INSIDER / SECRET, FOUNDATION / PLOT_NEED. 플롯을 위해 넣은 설정에는 세계 안의 근거를 요구한다.
- **정보 환경 층.** 메신저·메일·문서·검색·사진·계정마다 보존·삭제 흔적·권한·시각 기준을 정한다.
- **허점 정당화와 역방향 설계.** 사건을 먼저 정한 기획을 위해 추가했다.

### Canon schema v2

- **헌장(charter).** 현실 거리, 핵심 전제, 세계 범위, 조사 현재 시점.
- **TimeSpec.** 정확한 시각, 불확실 구간 `{earliest, latest, label}`, 미정을 표현한다. 시간 검사는 모순이 확실할 때만 오류를 낸다.
- **새 종류.** Location(장소 계층·이동 시간), HistoryEvent(실제와 통념), Term(허용·금지 별칭).
- **새 구조화 필드.** 보존·권한·기록 유형, 조직의 설립·구조·보상, 인물의 소속·거주지·일과·말투, 사건 장소, 기록의 서비스·현재 상태.
- **새 링크.** `entails`(2차 파급), `exploits`(사건이 이용한 허점). `knows` 링크는 폐지 예정이며 경고만 낸다.
- **규칙 ID 22종.** TIME, GRAPH, SPACE, DATA, TERM, WORLD, FAIR, CLAIM, LINK. validate가 findings로 출력하므로 QA Issue로 그대로 옮길 수 있다.
- **새 명령.**
  - `canon.py bible`: 세계 바이블. 연표, 장소 트리, 조직, 규칙, 서비스별 보존·권한, 용어집, 공개도를 출력하고, 요일과 경과 기간을 계산한다.
  - `canon.py timeline --character`: 인물별 시간선.
  - `canon.py migrate`: v1 → v2 무손실 이관. 정확히 옮기지 못한 부분은 MIGRATION 이슈로 남긴다.
- **v1 호환.** v1 snapshot은 그대로 검증된다. 기존 객체의 관리 본문 렌더링은 바이트 단위로 같아서, 이미 만든 Notion 페이지가 사람 편집으로 오인되지 않는다.

### 다른 스킬

- **Character Knowledge Builder.**
  - 프로필과 말투 참조 문서를 추가했다.
  - 대필·사칭을 설계하는 절차를 넣었다.
  - 거짓말은 숨기는 진실과 그 시점의 지식을 가리키도록 했다.
- **Mystery Plot Builder.**
  - 사건 장소와 지속 시간을 적는다.
  - 기록이 어느 서비스에 저장됐는지와 조사 현재 시점의 상태를 적는다.
  - 공정성 절을 추가했다: 해답 전 노출표, 레드 헤링의 세계 내 원인, 마지막 문서 금지.
- **Mystery Continuity Auditor.**
  - 변경 검토·전체 검토·완료 판정의 세 방식을 두었다.
  - 검사 항목별 규칙 ID를 대응시켰다.
  - 심각도는 대상이 얼마나 핵심인지로 정한다.
  - 보고 형식을 고정했다.
- **Mystery Production Orchestrator.**
  - 세계 우선·사건 우선 진행 방식을 두었다.
  - 세계 질문 사다리를 넣었다.
  - 고정점이 바뀌면 다음 질문 전에 정합성 체크포인트를 거친다.
  - 완료 기준에 세계관 조건을 추가했다.

### Notion 템플릿 1.1.0

- **새 선택지와 속성.** Kind에 Location·HistoryEvent·Term을 추가했다. Depth·Visibility·Origin 속성과 Relation 선택지 `entails`·`exploits`를 추가했다.
- **세계관 페이지 뷰.** 연표(Event Time 순서), 장소, 정보 환경, 용어집 뷰를 추가했다.
- **Event Time.** 연표 날짜로도 쓴다. 조직 설립, 서비스 출시, 규칙 유효 기간이 들어간다.
- **`notion_plan.py upgrade`.** 1.0.0 프로젝트에 속성과 선택지를 추가만 하는 요청을 만든다. 기존 선택지와 색은 보존하고, 타입 충돌이 있으면 멈춘다.
- **본문 템플릿.** 페이지 본문과 기획서 템플릿의 세계관·인물·조사 절을 세분화했다.

### 수정

- 진상(Fact)을 바꿨을 때 그것을 뒷받침하던 기록이 재검토 대상에 오르지 않던 문제.
- 원인 사건보다 이른 기록, 획득 경로보다 이른 지식, 시간 역전, 인과 순환을 검사하지 않던 문제.
- 문서가 요구하는 필드(`cannot_know`, Claim의 관련 Fact, 규칙 유효 시점)가 데이터 계약에 없던 문제.
- 잘못된 입력에서 검증기가 예외로 멈추던 문제. 예: null `witness`, 목록·객체가 들어간 선택값.
- 같은 구역 안의 두 장소를 이동 거리 0으로 계산하던 문제.

### 패키지와 품질

- `validate_suite.py`에 검사를 추가했다.
  - 5개 스킬의 공통 계약 사본이 서로 달라지지 않았는지 확인한다.
  - 참조 문서의 TODO를 검사한다.
  - 스킬 문서와 코드의 필드명을 데이터 계약과 양방향으로 대조한다.
- `agents/openai.yaml`의 기본 요청을 스킬별로 바꿨다.
- 테스트를 21개에서 65개로 늘렸다. fixture를 `tests/fixtures`로 분리했다.
- 행동 평가 시나리오 11개(`evals/`)를 추가했다. 이 가운데 8개는 정답 규칙을 테스트가 검사기와 대조한다.

## 1.0.0

첫 공개 버전. 6개 스킬, Canon schema v1, Notion 템플릿 1.0.0, 동기화·충돌·부분 실패 복구 절차를 담았다.
